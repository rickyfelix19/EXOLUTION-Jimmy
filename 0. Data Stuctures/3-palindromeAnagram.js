// 3. Code to check whether two input strings are anagram or not as well as palindrome.
function palindromeAnagram() {
	return;
	// check letter by letter
	// if palindrome word is backward to forward
	// if anagram letter is the same but rearranged

	// if each letter is the same but rearranged then anagram
	// use a specific algorithm ->
	// if each letter from front to back and back to front is the equal then it is a palindrome
	// use a specific algorithm ->
}
